from django.urls import include, path
from query_api import views


urlpatterns = [
    #path('query/', views),
    #path('result/', views),
    #path('ping/', views),
    #path('history/', views),

]